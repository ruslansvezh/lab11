#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	setlocale(0, "");
	int N,K=1,sum=1;
	{
		cout << "Введите N : " << endl;
		cin >> N;
		while (sum <= N)
		{
			K++;
			sum = sum + K;
		}
		cout << "Наименьшее K= " << K << endl;
		cout << "Сумма= " << sum;
	}
}
